[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "24-hotel-revervation-ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "imbalanced-learn>=0.14.1",
    "kagglehub>=1.0.1",
    "lightgbm>=4.6.0",
    "matplotlib>=3.10.9",
    "mlflow>=3.12.0",
    "numpy>=2.4.2",
    "pandas>=2.2,<3",
    "protobuf>=4.25.3,<6",
    "pyyaml>=6.0.3",
    "scikit-learn>=1.8.0",
    "seaborn>=0.13.2",
    "setuptools>=82.0.1",
    "statsmodels>=0.14.6",
    "xgboost>=3.2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src", "config", "utils"]
